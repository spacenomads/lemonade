{
  "name": "lemonade",
  "version": "1.0.0",
  "description": "Sidiostedalimones.com 2021 version 🦄",
  "main": "index.js",
  "scripts": {
    "build": "(npm run clean || true) && eleventy",
    "build:pro": "cross-env MODE=pro npm run build",
    "watch": "eleventy --watch",
    "serve": "npm run clean && eleventy --serve",
    "start": "cross-env MODE=dev npm-run-all --parallel assets serve",
    "assets": "gulp go",
    "clean": "gulp clean",
    "debug": "DEBUG=* eleventy",
    "publish": "rm -rf docs && npm run clean && eleventy && cp -r dist docs"
  },
  "keywords": [
    "lemonade",
    "blog",
    "if god gives you lemons"
  ],
  "author": "Carlos Mañas",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@11ty/eleventy-navigation": "^0.3.2",
    "@hail2u/css-mqpacker": "github:hail2u/node-css-mqpacker",
    "cross-env": "^7.0.3",
    "del": "^5.1.0",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^8.0.0",
    "gulp-clean-css": "^4.3.0",
    "gulp-postcss": "^9.0.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^5.0.0",
    "html-minifier": "^4.0.0",
    "node-sass": "^6.0.1",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.2.15"
  },
  "browserslist": [
    "last 2 versions",
    "ie >= 10"
  ]
}
